import React from 'react'

export const metadata = {
  title: "Form Feedback - Campus Insights System",
  description: "",
};

export default function layout({children}) {
  return (
    <>
     {children} 
    </>
  )
}
