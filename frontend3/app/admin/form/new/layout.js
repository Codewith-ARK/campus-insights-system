import React from 'react'

export const metadata = {
  title: "Create New Form - Campus Insights System",
  description: "",
};

export default function layout({children}) {
  return (
    <>
      {children}
    </>
  )
}
